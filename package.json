{
  "name": "onscrollpathanimations",
  "homepage": "https://Pratush-Tiwari.github.io/React-svg-portfolio",
  "version": "1.0.0",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "description": "",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "parcel src/index.html --open",
    "clean": "rm -rf dist/*",
    "build:parcel": "parcel build src/index.html --no-content-hash --no-source-maps --public-url ./",
    "build": "npm run clean && npm run build:parcel"
  },
  "repository": {
    "type": "git"
  },
  "keywords": [],
  "author": "Codrops",
  "license": "MIT",
  "bugs": {
  },
  "devDependencies": {
    "parcel": "^2.6.0"
  },
  "dependencies": {
    "@studio-freight/lenis": "^0.1.2",
    "gh-pages": "^4.0.0",
    "gsap": "^3.10.4",
    "imagesloaded": "^5.0.0"
  }
}
